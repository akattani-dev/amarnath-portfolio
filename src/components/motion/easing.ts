/**
 * One curve for every entrance on the site. Declared as a mutable tuple so it
 * satisfies motion's cubic-bezier `Easing` type without a cast.
 */
export const EASE: [number, number, number, number] = [0.22, 1, 0.36, 1];
